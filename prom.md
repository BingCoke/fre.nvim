- ../的时候, cursor没有放在我们原来的dir位置上, 而是出现在top ../的位置
- 怎么../点击之后 还会展开我们原来子的path? 有必要这个复杂度吗, 不要做多余的事情
- 处理好继承关系, 就是说instace不要有任何继承的感觉, 就是不能有任何继承的字段, 一切靠传入opt, 不要传入某个instace的来自的位置 , 这个继承只是一个方便业务理解的说法, 在整个instace生命周期中是没有这个感觉的

- :w 之后我们会开启一个falot windows 用于确认, 但是我们这个确认的float面板发现问题,不会自动跳到这个buffer 
- 有没有类似oil那种, 快速确认的方式, 就是oil好像创建文件的时候是直接就创建了, 忘记是什么opt了, 除非是删除什么的危险操作才会弹出确认, 包括我们refresh的时候也是, 其实可能也没必要去确认, 这个float 确认是 action里面的操作把, 可以不用的, 应该是这么一个opt的, 就是说配置这个opt, 那么可以快速跳过一些步骤, 默认是不跳过的, 但是如果配置了之后就可以跳过

- float layout 的select, 没有考虑到 就是说select 是否关闭,  包括select, select tab , select split, 都没有考虑过, select是直接在当前float选择

- expand 的操作的时候, 如果这个仓库目录的children 有且仅只有一个dir的时候, 那么可以递归继续把这个dir expand, 就比如java的 com/xxx/xx/src 这个时候可以递归直接全部expand就好了, 这个也作为一个opt, 就是说当展开的dir有且只有一个dir的时候是否可以直接递归继续展开
- 还有加一个action ,就是unexpand, 就是把这个目录下root下所有的expand的dir全部合并, zr? 还是什么快捷键, 你配置一下
> 顺带一个一问题, 一个dir展开然后再unexpand之后, 是否还会watch 这个dir?, 就是说优化是否足够, 


- 编写文档, 怎么在lualine/ tabby 中正确的配置名字还有路径
    - lualine 使用的是当前全局的相对路径, 如果文件路径不在全局的cwd的位置, 那么就配置为 真实的绝对路径, 那么就是使用的是
    - tabby只需要展示一个Fre即可



- 编写keymap , 快速跳转cursur到当前行的 path的开头, [gp] 使用这个keymap, 表示快速跳转到当前行的path , 如果当前行是正在编辑的/ 或者无法解析的, 那么就什么也不做, 也不报错, 只是尽力而为的处理


- fs.lua 中的文件搜索,是串行的, 当目录中文件过多时, 会有很大的延迟, 所以需要优化文件搜索的方式





